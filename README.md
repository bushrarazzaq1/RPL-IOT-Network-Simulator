# RPL-IOT-Network-Simulator
RPL (Routing Protocol for Low-Power and Lossy Networks) is a simulator that creates a DODAG tree of nodes, where each node tracks its distance and energy relative to its parent, and data packets can be routed to the sink while draining the energy of all nodes along the route.
